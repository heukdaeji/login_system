<!DOCTYPE html>
<html lang="en">
<head>
    <title>LOGIN EXPOSED</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;600&display=swap" rel="stylesheet">
    <!--Stylesheet-->
    <style media="screen">
        *,*:before, *:after{
            padding: 0;
            margin: 0;
            box-sizing: border-box;
            font-family: 'Poppins',sans-serif;
            letter-spacing: 0.5px;
            outline: none;
            border: none;
        }
        body{
            background-color: #080710;
        }
        .background{
            width: 430px;
            height: 520px;
            position: absolute;
            transform: translate(-50%,-50%);
            left: 50%;
            top: 50%;
        }
        .background .shape{
            height: 200px;
            width: 200px;
            position: absolute;
            border-radius: 50%;
        }
        .shape:first-child{
            background: linear-gradient(
                #1845ad,
                #23a2f6
            );
            left: -80px;
            top: -80px;
        }
        .shape:last-child{
            background: linear-gradient(
                to right,
                #ff512f,
                #f09819
            );
            right: -30px;
            bottom: -80px;
        }
        #top-bar a{
            width: 252px;
            font-size: 40px;
            font-weight: 500;
            line-height: 9vh;
            color: #36393F;
            margin: 0px 0px 0px 20px;
        }
        #top-bar ul {
            display: inline;
            margin: 0;
            padding: 0;
        }
        #top-bar #Auth ul li {
            text-align: center;
            width: 170px;
            height: 100%;
            font-family: 'Poppins',sans-serif;
            background-color: #2E3338;
            font-size: 20px;
            backdrop-filter: blur(10px);
            border-radius: 5px;
            line-height: 8vh;
            display: inline-block;
        }
        #top-bar #Auth ul li a {
            font-size: 30px;
            margin: 0 0;
            color: #3BA55D;
        }
        #top-bar #Auth ul li:hover {
            background-color: #202225;
        }

        #top-bar #Auth ul li:hover ul {
            display: block;
        }

        #top-bar #Auth ul li ul {
            width: 170px;
            display: none;
        }

        #top-bar #Auth ul li ul li {
            background: #202225;
            display: block;
        }

        #top-bar #Auth ul li ul li:hover {
            background: rgb(27, 136, 209);
        }

        #top-bar #Auth ul li ul li button {
            display: block !important;
            cursor: pointer;
            background-color: transparent;
            color: #1845ad;
            font-size: 25px;
            justify-content: center;
            margin: auto;
        }

        #top-bar #loginButton {
            text-align: center;
            width: 170px;
            height: 100%;
            font-family: 'Poppins',sans-serif;
            background-color: #24e424;
            font-size: 20px;
            border: 2px solid #40444B;
            backdrop-filter: blur(10px);
            border-radius: 5px;
            cursor: pointer;
            display: inline-block;
        }

        #top-bar {
            height: 10vh;
            width: 100vw;
            background-color: rgba(255,255,255,0.13);
            border: 2px solid #40444B;
            backdrop-filter: blur(10px);
            border-radius: 5px;
            display: flex;
            justify-content: space-between;
        }

        #homebtn {
            background-color: #2E3338;
            width: 170px;
            font-size: 30px;
            cursor: pointer;
            color: yellow;

        }
        #buttons {
            position: absolute;
            width: 100%;
            padding: 40vh 0;
            text-align: center;
        }
        #buttons div {
            display: inline-block;
        }
        #buttons div button {
            background-color: #202225;
            width: 30vw;
            border-radius: 10px;
            height: 10vh;
            font-size: 40px;
            color: #23a2f6;
        }
        /* //아까 디코에 올리셨으니까 갈아엎어볼게요 ok */
    </style>
</head>
    <body>
        <div id="top-bar">
            <a>Sussy Home</a>
            <button id="homebtn">Home</button>
            <div id="Auth">
                <ul>
                    <li>
                        <a id="userName"></a>
                        <ul>
                            <li><button id="LogoutBtn">Logout</button></li>
                            <li><button id="userInfoBtn">userInfo</button></li>
                        </ul>
                    </li>
                </ul>
            </div>
            <div id="UnAuth">
                <button id="loginButton">Login</button>
            </div>
        </div>
        <div class="background">
            <div class="shape"></div>
            <div class="shape"></div>
        </div>
        <script type="text/javascript" src="quizsolve.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    </body>
</html>
